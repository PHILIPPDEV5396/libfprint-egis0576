# --- Build artifacts ---
builddir/
build/
*.o
*.a
*.so
*.so.*
packaging/debian/build-output/
packaging/ubuntu-tod/build-output/
*.deb
*.ddeb
*.changes
*.buildinfo
packaging/ubuntu-tod/debian/.debhelper/
packaging/ubuntu-tod/debian/files
packaging/ubuntu-tod/debian/*.debhelper.log
packaging/ubuntu-tod/debian/*.substvars
packaging/ubuntu-tod/debian/debhelper-build-stamp
packaging/ubuntu-tod/debian/libfprint-2-tod1-egis0576/

# --- Egis' copyrighted material — reverse-engineering reference ONLY, never commit ---
# The original Windows driver binary and any full decompilation of it are Egis
# Technology's property. They are development/reference material that stays local.
*.dll
decomp*.txt
*.decomp

# --- Personal biometric data — NEVER commit captured fingerprints ---
*.bin
*.pgm
*.raw
live_frames/
session_*/
boot*_enroll/
egis_dbg/
cleanroom/livedata/
baseline*.bin

# --- Editor / OS cruft ---
*.swp
*~
.DS_Store
.vscode/
.idea/
